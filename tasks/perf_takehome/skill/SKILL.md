# perf take-home: planner skill

Optimize `KernelBuilder.build_kernel` for the fewest simulated clock cycles. One bead
per cycle milestone: beat the updated starting point first, then drive the count down
past each published bar. The eval reports the live cycle count and which bars are
still unmet, and the `tests/` folder is read-only.

<!-- coverage:start -->
m18532
m2164
m1790
m1487
m1363
<!-- coverage:end -->
